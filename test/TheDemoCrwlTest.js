/**
 * Test for TheDemoCrwl.
 * Runs with mocha.
 */
'use strict'

const TheDemoCrwl = require('../lib/TheDemoCrwl')
const { ok, equal } = require('assert')

describe('the-demo-crawler', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    ok(TheDemoCrwl)
  })
})

/* global describe, before, after, it */
